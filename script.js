let char = "bob choPra";

console.log(`using the method .toUpperCase() ${char.toUpperCase()} `);
console.log(`using the method .toLowerCase() ${char.toLowerCase()}`)
console.log(`using the method .starsWith() ${char.startsWith("b")}`);
